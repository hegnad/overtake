export interface ErgastDriver {
    name: string;
    firstName: string;
    lastName: string;
}


export interface OpenF1Driver {
    fullName: string;
    headshotUrl: string;
}

export interface CombinedDriver {
    name: string;
    headshotUrl: string;
}